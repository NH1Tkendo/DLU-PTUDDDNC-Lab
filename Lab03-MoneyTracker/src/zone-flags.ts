/**
 * This file is used to configure the behavior of Angular's zone.js
 * Zone.js is a library that implements Zones for JavaScript
 */

// Disable Zone.js for specific operations if needed
// (window as any).__Zone_disable_requestAnimationFrame = true;
// (window as any).__Zone_disable_on_property = true;
